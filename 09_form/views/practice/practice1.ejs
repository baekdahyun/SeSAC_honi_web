<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>실습문제1</title>
  </head>
  <body>
    <h1>실습문제1</h1>
    <form action="/getPrForm" method="get">
      <label
        >이름
        <input type="text" name="name" />
      </label>

      <!-- 성별 -->
      <fieldset>
        <legend>성별</legend>
        <input type="radio" name="mf" id="male" value="남자" />
        <label for="mf1">남자</label>
        <input type="radio" name="mf" id="female" value="여자" />
        <label for="mf2">여자</label>
      </fieldset>
      <br />

      <!-- 생년월일 -->
      <fieldset>
        <legend>생년월일</legend>
        <select name="birth" id="birthYear">
          <% for(let i = 1980; i<=2010; i++ ){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%>
        </select>
        <span>년</span>
        <select name="birth" id="birthMonth">
          <% for(let i = 1; i<=12; i++ ){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%>
        </select>
        <span>월</span>
        <select name="birth" id="birthDay">
          <% for(let i = 1; i<=31; i++ ){%>
          <option value="<%=i%>"><%=i%></option>
          <%}%>
        </select>
        <span>일</span>
      </fieldset>
      <br />

      <!-- 관심사 -->
      <fieldset>
        <legend>관심사</legend>
        <label>
          <input type="checkbox" name="like" value="여행" />
          여행
        </label>
        <label>
          <input type="checkbox" name="like" value="패션" />
          패션
        </label>
        <label>
          <input type="checkbox" name="like" value="음식" />
          음식
        </label>
      </fieldset>
      <br />

      <button type="submit">회원가입</button>
    </form>
  </body>
</html>
